package `in`.workindia.rapidwebview.datamodels

enum class DownloadLocation {
    EXTERNAL_FILES,   // shared but app-specific
    PUBLIC_DOWNLOADS  // public download directory accessible to all apps
}